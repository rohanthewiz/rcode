# Example environment configuration for RCode

# Anthropic API Configuration
# Use MSG_PROXY if you have a proxy server for the Anthropic API
# MSG_PROXY=http://localhost:8001

# TLS Configuration
# Set to "true" to enable HTTPS
RCODE_TLS_ENABLED=false

# HTTPS port (default: :8443)
# RCODE_TLS_PORT=:8443

# Certificate paths (relative to project root)
# RCODE_TLS_CERT=certs/localhost.crt
# RCODE_TLS_KEY=certs/localhost.key